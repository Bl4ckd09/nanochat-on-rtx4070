# Nanochat Next-Step Plan (d24, RTX 4070)
*Updated: 2026-03-13*

## Current Snapshot (locked baseline)
- Base model: `d24_asp48_track` step `615173` (target ratio 24 complete)
- Final base checkpoint: `~/.cache/nanochat/base_checkpoints/d24_asp48_track/model_615173.pt`
- Base eval (`2026-03-12`):
  - `val bpb: 0.908073`
  - `CORE metric: 0.1494`
- No active train/eval process currently running.

## Objective
Maximize final model capability on one RTX 4070 (12GB) by pushing the strongest lever first:
1) more base pretraining, 2) SFT sweep on strongest base, 3) optional RL polish.

---

## Stage 1 (Highest ROI): Continue Base Training Beyond Ratio 24
Run from the same model tag so auto-resume picks up from `step 615173`.

### 1A) Push to ratio 32 first
```bash
bash ~/nanochat-learn/scripts/run_base_d24_asp48_maxcore_safe.sh d24_asp48_track 32
```
- Expected finish from current step: about **5.5 days**.
- Script will auto-run final CORE/BPB eval and append to `nanochat/report_v2.md`.

### 1B) Then push to ratio 40
```bash
bash ~/nanochat-learn/scripts/run_base_d24_asp48_maxcore_safe.sh d24_asp48_track 40
```
- Additional time from ratio 32: about **5.5 days**.
- Total from current step to ratio 40: about **11.1 days**.

### Runtime model used for ETA
- Measured from your completed run: ~`2.33s/step` effective.
- Ratio targets with current config (`TOTAL_BATCH_SIZE=16384`):
  - ratio 30: step `768966`
  - ratio 32: step `820230`
  - ratio 36: step `922759`
  - ratio 40: step `1025288`

Decision gate after ratio 32:
- If CORE and bpb both improve meaningfully, continue to ratio 40.
- If gains flatten hard, still continue to ratio 40 if your objective is absolute max quality (not fastest turnaround).

---

## Stage 2: SFT Candidate Sweep on the Strongest Base
Use the best base step (prefer ratio-40 step if completed, else ratio-32 step).

Assume:
- `BASE_TAG=d24_asp48_track`
- `BASE_STEP=1025288` (or `820230` if stopping at ratio 32)

### Candidate A: LoRA SFT (fast, low VRAM risk)
```bash
cd ~/nanochat-learn/nanochat
source .venv/bin/activate
TS=$(date +%F_%H%M)
OUT=d24_r40_lora_${TS}

PYTHONUNBUFFERED=1 TORCH_COMPILE_DISABLE=1 \
python -u -m scripts.chat_sft \
  --model-tag d24_asp48_track --model-step 1025288 \
  --output-tag ${OUT} --run ${OUT} \
  --device-type cuda --dtype bfloat16 \
  --lora --lora-rank 64 --lora-alpha 128 --lora-dropout 0.0 --lora-lr 1e-4 \
  --max-seq-len 1536 --device-batch-size 1 --total-batch-size 8192 \
  --num-iterations 1000 --eval-every 50 --eval-tokens 524288 \
  --max-grad-norm 1.0 --keep-best-k 5 --no-save-optimizer \
  |& tee ~/nanochat-learn/notes/sft_${OUT}.log
```

### Candidate B: Full SFT (higher adaptation ceiling, higher VRAM risk)
```bash
cd ~/nanochat-learn/nanochat
source .venv/bin/activate
TS=$(date +%F_%H%M)
OUT=d24_r40_full_${TS}

PYTHONUNBUFFERED=1 TORCH_COMPILE_DISABLE=1 \
python -u -m scripts.chat_sft \
  --model-tag d24_asp48_track --model-step 1025288 \
  --output-tag ${OUT} --run ${OUT} \
  --device-type cuda --dtype bfloat16 \
  --adamw-only --weight-decay 0.0 \
  --gradient-checkpoint \
  --max-seq-len 1536 --device-batch-size 1 --total-batch-size 7680 \
  --num-iterations 1000 --eval-every 50 --eval-tokens 524288 \
  --max-grad-norm 1.0 --keep-best-k 5 --no-save-optimizer \
  |& tee ~/nanochat-learn/notes/sft_${OUT}.log
```

Notes:
- On your 4070, 1000-step LoRA SFT is typically short (about 10-20 minutes). Eval is the long part.
- Keep `--output-tag` unique every run. Never reuse old d18 tags.

---

## Stage 3: Evaluate and Select Champion
Run identical evaluation settings for each SFT candidate:
```bash
~/nanochat-learn/scripts/run_chat_eval_confirm_1k.sh <model_group>/best 1000 1000
```
Examples:
```bash
~/nanochat-learn/scripts/run_chat_eval_confirm_1k.sh d24_r40_lora_YYYY-MM-DD_HHMM/best 1000 1000
~/nanochat-learn/scripts/run_chat_eval_confirm_1k.sh d24_r40_full_YYYY-MM-DD_HHMM/best 1000 1000
```

Then run extended checks on the winner:
```bash
~/nanochat-learn/scripts/run_chat_eval_pack.sh <winner_group>/best 1000
```

Expected eval wall-time:
- `confirm_1k` run: typically a few hours on single 4070 (GSM8K pass@8 dominates).

---

## Stage 4 (Optional): RL Polish for Math
Only do this if GSM8K is still below your target after SFT and general metrics are acceptable.

```bash
cd ~/nanochat-learn/nanochat
source .venv/bin/activate
TS=$(date +%F_%H%M)
RUN=d24_r40_rl_${TS}

PYTHONUNBUFFERED=1 \
python -u -m scripts.chat_rl \
  --run ${RUN} \
  --device-type cuda --dtype bfloat16 \
  --model-tag <winner_group>/best --model-step 1000 \
  --num-epochs 1 \
  --device-batch-size 1 \
  --examples-per-step 8 \
  --num-samples 4 \
  --max-new-tokens 256 \
  --temperature 0.7 --top-k 50 \
  --eval-every 20 --eval-examples 200 --save-every 50 \
  |& tee ~/nanochat-learn/notes/rl_${RUN}.log
```

Re-run `run_chat_eval_confirm_1k.sh` after RL and keep RL only if it improves target metrics without unacceptable regressions.

---

## Operations Rules (important)
- Keep `d24_asp48_track` as the only base tag for continuation to avoid broken resume logic.
- Archive stale checkpoints if needed, but keep ratio milestones (`615173`, `820230`, `1025288` once available).
- Keep `--keep-best-k` small for SFT (`1` to `5`) to control disk growth.
- Log every run with `tee` into `~/nanochat-learn/notes/`.

## Recommended Immediate Action
Start Stage 1A now:
```bash
bash ~/nanochat-learn/scripts/run_base_d24_asp48_maxcore_safe.sh d24_asp48_track 32
```
