# SFT Plan (d24_r32 base) - 2026-03-19

## Goal
Run highest-value SFT on RTX 4070 from base checkpoint `d24_asp48_track` step `820230`.

## Key Constraint
For `scripts.chat_sft`, `total_batch_size` must be divisible by `device_batch_size * max_seq_len`.
- With `device_batch_size=1` and `max_seq_len=1536`, valid token batches include `1536 * k`.
- Use `total_batch_size=7680` (k=5).

## Stage 1 (Primary Run)
- Mode: LoRA
- Base: `--model-tag d24_asp48_track --model-step 820230`
- Params:
  - `--max-seq-len 1536`
  - `--device-batch-size 1`
  - `--total-batch-size 7680`
  - `--num-iterations 1000`
  - `--lora-rank 64 --lora-alpha 128 --lora-lr 1e-4`
  - `--eval-every 50 --eval-tokens 524288`
  - `--keep-best-k 5 --no-save-optimizer`

## Stage 2 (Fallback if OOM)
- Retry with `--max-seq-len 1280 --total-batch-size 7680`.
- If still unstable, use `--max-seq-len 1024 --total-batch-size 8192`.

## Monitoring
- Log file in `~/nanochat-learn/notes/sft_*.log`
- Hourly summary in `~/nanochat-learn/notes/sft_*_hourly.log`
- W&B project: `nanochat-sft`
- Watch early health in first ~100 steps:
  - OOM/restarts
  - loss trend stability
  - first validation bpb at step 50

## Post-Run
1. Run confirm eval:
   - `~/nanochat-learn/scripts/run_chat_eval_confirm_1k.sh <OUTPUT_TAG>/best 1000 1000`
2. Compare against base and prior SFT runs.
3. Decide next: longer SFT vs RL vs another base-ratio run.
